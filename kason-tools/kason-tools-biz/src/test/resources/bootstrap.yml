spring:
  application:
    name: kason-tools-biz
  cloud:
    bootstrap:
      enabled: false
    config:
      enabled: false
      fail-fast: false
      discovery:
        enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

