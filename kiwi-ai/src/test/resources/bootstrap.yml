jasypt:
  encryptor:
    password: ${KIWI_ENC_PASSWORD}
    poolSize: 2

server:
  port: ${PORT:0}

spring:
  application:
    name: kiwi-ai
  profiles:
    active: test
  cloud:
    config:
      enabled: false
    loadbalancer:
      ribbon:
        enabled: false

eureka:
  client:
    enabled: false