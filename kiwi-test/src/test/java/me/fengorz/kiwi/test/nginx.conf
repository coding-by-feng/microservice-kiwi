server {
    listen 80;
    server_name fly.fengorz.me;

    access_log  /var/log/nginx/fly.access.log;
    error_log /var/log/nginx/fly.error.log;
    root /usr/share/nginx/html;

    location / {
            index  index.HTML;
    }
}