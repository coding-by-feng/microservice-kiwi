# 数据源
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ENC(KH/GJ05cpFBSAV5u8IruRw==)
    password: ENC(Dh6vV+rX9HyZgiOP83BIiQAZ1+9KOGVh)
    url: jdbc:mysql://${DB_IP}:3306/kiwi_db?allowPublicKeyRetrieval=trueuseUnicode=true&characterEncoding=UTF-8&autoReconnect=true&useSSL=false&serverTimezone=Pacific/Auckland
    hikari:
      max-lifetime: 60000
      connection-timeout: 60000
      maximum-pool-size: 10
  main:
    allow-bean-definition-overriding: true

# 直接放行URL
ignore:
  urls:
    - /actuator/**
    - /sys/user/info/*
    - /log/**
    - /sys/user/oneClickRegister
